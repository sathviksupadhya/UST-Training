eureka:
  client:
    serviceUrl:
      defaultZone: http://registry-sr:8763/eureka/

server:
  port: 9092

spring:
  application:
    name: Supplier
  datasource:
    url: jdbc:mysql://mysqldb-supp:3306/supplier
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

