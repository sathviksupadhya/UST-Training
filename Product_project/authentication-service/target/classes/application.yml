eureka:
  client:
    serviceUrl:
      defaultZone: http://registry-sr:8763/eureka/

server:
  port: 8090

spring:
  application:
    name: authentication-service
  datasource:
    url: jdbc:mysql://mysqldb-mydb:3306/mydb
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
